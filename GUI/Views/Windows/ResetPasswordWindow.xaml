<Window x:Class="GUI.Views.Windows.ResetPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI.Views.Windows"
        mc:Ignorable="d"
        Title="Đặt lại mật khẩu" Height="600" Width="450"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Roboto">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="GrayText" Color="#6B7280"/>

        <!-- Style cho các nút điều khiển cửa sổ (Minimize, Maximize, Close) -->
        <Style x:Key="WindowControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <TextBlock Text="{TemplateBinding Content}" 
                                       FontFamily="Segoe MDL2 Assets" 
                                       FontSize="12" 
                                       Foreground="{StaticResource GrayText}" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F3F4F6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho PasswordBox giống TextBox ở các màn hình khác -->
        <Style TargetType="PasswordBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#A5B4FC"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                <Setter TargetName="border" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Border chính tạo khung cửa sổ -->
    <Border Background="White" CornerRadius="20" BorderBrush="#E5E7EB" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.1" Color="Gray"/>
        </Border.Effect>

        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header (Back + Controls) -->
                <RowDefinition Height="*"/>
                <!-- Content -->
            </Grid.RowDefinitions>

            <!-- 1. Header: Back Button & Window Controls -->
            <Grid Grid.Row="0">
                <!-- Nút Quay lại -->
                <Button Command="{Binding BackToLoginCommand}" 
                        HorizontalAlignment="Left" VerticalAlignment="Top"
                        Background="Transparent" BorderThickness="0" Cursor="Hand">
                    
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <ContentPresenter/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Cụm nút điều khiển cửa sổ -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button Style="{StaticResource WindowControlButtonStyle}" Content="&#xE921;" Click="MinimizeButton_Click" ToolTip="Thu nhỏ"/>
                    <Button Style="{StaticResource WindowControlButtonStyle}" Content="&#xE922;" Click="MaximizeButton_Click" ToolTip="Phóng to"/>
                    <Button Style="{StaticResource WindowControlButtonStyle}" Content="&#xE8BB;" Click="CloseButton_Click" ToolTip="Đóng"
                            Foreground="#EF4444"/>
                </StackPanel>
            </Grid>

            <!-- 2. Main Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="340">

                <!-- Icon Minh Họa (Ổ khóa) -->
                <Border Background="#EFF6FF" Width="80" Height="80" CornerRadius="40" HorizontalAlignment="Center" Margin="0,0,0,25">
                    <TextBlock Text="&#xE72E;" FontFamily="Segoe MDL2 Assets" FontSize="36" Foreground="{StaticResource PrimaryBlue}" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Tiêu đề -->
                <TextBlock Text="Đặt lại mật khẩu" FontSize="26" FontWeight="Bold" Foreground="#111827" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <!-- Mô tả -->
                <TextBlock Text="Mật khẩu mới của bạn phải khác với mật khẩu đã sử dụng trước đó." 
                           FontSize="14" Foreground="{StaticResource GrayText}" TextWrapping="Wrap" TextAlignment="Center" 
                           Margin="0,0,0,30"/>

                <!-- Form nhập liệu -->
                <StackPanel Margin="0,0,0,25">
                    <TextBlock Text="Mật khẩu mới" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#374151"/>
                    <PasswordBox x:Name="NewPasswordBox" Margin="0,0,0,15"/>

                    <TextBlock Text="Xác nhận mật khẩu" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#374151"/>
                    <PasswordBox x:Name="ConfirmPasswordBox"/>
                </StackPanel>

                <!-- Nút Xác Nhận -->
                <Button Command="{Binding ConfirmResetPasswordCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" 
                        Content="Đổi mật khẩu"
                        Background="{StaticResource PrimaryBlue}" Foreground="White"
                        FontSize="16" FontWeight="Bold"
                        Height="50"
                        Cursor="Hand" Margin="0,10,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>
        </Grid>
    </Border>
</Window>