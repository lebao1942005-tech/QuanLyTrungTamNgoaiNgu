<UserControl x:Class="GUI.Views.Shared.TeacherSidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="240"
             Width="240" Background="White" FontFamily="Roboto">

    <UserControl.Resources>
        <!-- Style cho RadioButton menu (Copy từ SidebarView để đồng bộ giao diện) -->
        <Style TargetType="RadioButton" x:Key="SidebarRadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#64748B"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="15,4,15,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="TeacherSidebarGroup"/>
            <!-- GroupName riêng cho Teacher -->

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="{TemplateBinding Padding}">
                                <!-- Icon -->
                                <Image x:Name="Icon" 
                                       Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" 
                                       Width="20" Height="20" 
                                       Margin="0,0,15,0" 
                                       Opacity="0.6" 
                                       RenderOptions.BitmapScalingMode="HighQuality"/>

                                <!-- Text -->
                                <ContentPresenter VerticalAlignment="Center" TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Hover Effect -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EFF6FF"/>
                                <Setter Property="Foreground" Value="#1D4ED8"/>
                                <Setter TargetName="Icon" Property="Opacity" Value="1.0"/>
                            </Trigger>
                            <!-- Selected Effect -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#5683e9"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter TargetName="Icon" Property="Opacity" Value="1.0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style cho tiêu đề mục -->
        <Style TargetType="TextBlock" x:Key="SectionHeader">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="Margin" Value="35,25,0,10"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <!-- Dòng 1: Header -->
            <RowDefinition Height="*"/>
            <!-- Dòng 2: Menu (Chiếm hết khoảng trống) -->
            <RowDefinition Height="Auto"/>
            <!-- Dòng 3: Nút Đăng xuất (Luôn ở đáy) -->
        </Grid.RowDefinitions>

        <!-- 1. HEADER LOGO (Giữ nguyên) -->
        <Border Grid.Row="0" Background="#EFF6FF" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="25,0,0,0">
                <TextBlock Text="Star Edu" 
                           FontSize="18" 
                           FontWeight="ExtraBold" 
                           Foreground="#1D4ED8"/>
            </StackPanel>
        </Border>

        <!-- 2. MENU ITEMS (Giữ nguyên, chỉ thêm Grid.Row="1") -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" PanningMode="VerticalOnly">
            <StackPanel Margin="0,15,0,20">

                <TextBlock Text="GIẢNG DẠY" Style="{StaticResource SectionHeader}"/>

                <!-- NÚT LỚP HỌC -->
                <RadioButton Content="Lớp học" 
                             Style="{StaticResource SidebarRadioButton}" 
                             Tag="/Resources/Images/classroom-icon.png"
                             IsChecked="{Binding DataContext.IsClassPage, RelativeSource={RelativeSource AncestorType={x:Type Window}}, Mode=TwoWay}"
                             Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                             CommandParameter="ClassManagement"/>

            </StackPanel>
        </ScrollViewer>

        <!-- 3. NÚT ĐĂNG XUẤT (Thêm mới vào Grid.Row="2") -->
        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <!-- Đường kẻ mờ ngăn cách -->
            <Border BorderBrush="#F1F5F9" BorderThickness="0,1,0,0" Margin="15,0,15,10"/>

            <RadioButton Content="Đăng xuất" 
                         Style="{StaticResource SidebarRadioButton}" 
                         Tag="/Resources/Images/logout-icon.png"
                         Foreground="#EF4444" 
                         Command="{Binding DataContext.LogoutCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                         CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}"/>
        </StackPanel>

    </Grid>
</UserControl>