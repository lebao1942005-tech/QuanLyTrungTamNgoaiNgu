<UserControl x:Class="GUI.Views.Pages.TeacherManagement.AddTeacherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Width="500" Height="Auto"
             Background="Transparent" FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1D4ED8"/>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Container chính -->
    <Border Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2" Color="Gray"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Thêm giáo viên mới" FontSize="20" FontWeight="Bold" Foreground="#111827" VerticalAlignment="Center"/>
                <Button Command="{Binding CancelAddTeacherCommand}" 
                        Content="&#xE711;" FontFamily="Segoe MDL2 Assets" 
                        FontSize="12" Width="30" Height="30" 
                        Background="Transparent" BorderThickness="0" 
                        HorizontalAlignment="Right" Cursor="Hand"/>
            </Grid>

            <!-- 2. Form Fields -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top">

                <!-- Họ tên -->
                <TextBlock Text="Họ và tên" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                <TextBox Text="{Binding NewTeacherName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <!-- SĐT & Email -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <TextBox Text="{Binding NewTeacherPhone, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Email (Tài khoản đăng nhập)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <TextBox Text="{Binding NewTeacherEmail, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <!-- [CẬP NHẬT] Bộ môn (Thay ComboBox bằng List CheckBox) -->
                <TextBlock Text="Bộ môn giảng dạy (Chọn nhiều)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>

                <Border BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="10" Background="#F9FAFB" Height="120" Margin="0,0,0,15">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <!-- ItemsControl duyệt qua SubjectSelectionList -->
                        <ItemsControl ItemsSource="{Binding SubjectSelectionList}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <!-- WrapPanel để các checkbox dàn hàng ngang nếu đủ chỗ -->
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <!-- Binding vào class SelectableItem -->
                                    <CheckBox Content="{Binding Name}" 
                                              IsChecked="{Binding IsSelected}"
                                              Margin="0,0,15,10"
                                              Cursor="Hand"
                                              Foreground="#374151"
                                              FontSize="14"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- Note nhỏ về tài khoản -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                    <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" Foreground="#F59E0B" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="Hệ thống tự tạo tài khoản với Username là Email và mật khẩu mặc định '123'." 
                               Foreground="#6B7280" FontSize="12" FontStyle="Italic"/>
                </StackPanel>

            </StackPanel>

            <!-- 3. Footer Buttons -->
            <Grid Grid.Row="2" Margin="0,25,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Hủy bỏ" Command="{Binding CancelAddTeacherCommand}" 
                        Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Foreground="#374151" 
                        Height="40" Margin="0,0,5,0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Lưu giáo viên" Command="{Binding SaveNewTeacherCommand}" Grid.Column="1" 
                        Background="{StaticResource PrimaryBlue}" Foreground="White" BorderThickness="0" 
                        Height="40" Margin="5,0,0,0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>