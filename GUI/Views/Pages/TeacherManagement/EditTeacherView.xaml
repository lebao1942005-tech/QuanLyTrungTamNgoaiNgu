<UserControl x:Class="GUI.Views.Pages.TeacherManagement.EditTeacherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="500" Height="Auto"
             Background="Transparent" FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1D4ED8"/>

        <!-- Style cho TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <!-- Trigger để đổi màu khi ReadOnly -->
            <Style.Triggers>
                <Trigger Property="IsReadOnly" Value="True">
                    <Setter Property="Background" Value="#F3F4F6"/>
                    <Setter Property="Foreground" Value="#6B7280"/>
                    <Setter Property="BorderBrush" Value="#D1D5DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
        </Style>
    </UserControl.Resources>

    <!-- Container chính -->
    <Border Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2" Color="Gray"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Header -->
                <RowDefinition Height="*"/>
                <!-- Form -->
                <RowDefinition Height="Auto"/>
                <!-- Footer Buttons -->
            </Grid.RowDefinitions>

            <!-- 1. Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Chỉnh sửa thông tin giáo viên" FontSize="20" FontWeight="Bold" Foreground="#111827" VerticalAlignment="Center"/>
                <Button Command="{Binding CancelEditTeacherCommand}" 
                        Content="&#xE711;" FontFamily="Segoe MDL2 Assets" 
                        FontSize="12" Width="30" Height="30" 
                        Background="Transparent" BorderThickness="0" 
                        HorizontalAlignment="Right" Cursor="Hand"/>
            </Grid>

            <!-- 2. Form Fields -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top">

                <!-- Họ tên -->
                <TextBlock Text="Họ và tên" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                <TextBox Text="{Binding EditingTeacherName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <!-- SĐT & Email -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Số điện thoại" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <TextBox Text="{Binding EditingTeacherPhone, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Email (Tài khoản)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <!-- ĐÃ KHÓA: IsReadOnly="True" -->
                        <TextBox Text="{Binding EditingTeacherEmail, Mode=OneWay}" 
                                 IsReadOnly="True"
                                 ToolTip="Không thể thay đổi email vì đây là tài khoản đăng nhập hệ thống."/>
                    </StackPanel>
                </Grid>

                <!-- Bộ môn -->
                <TextBlock Text="Bộ môn giảng dạy" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                <ComboBox Text="{Binding EditingTeacherSubject, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsEditable="True">
                    <ComboBoxItem Content="IELTS"/>
                    <ComboBoxItem Content="TOEIC"/>
                    <ComboBoxItem Content="Tiếng Nhật"/>
                    <ComboBoxItem Content="Tiếng Trung"/>
                    <ComboBoxItem Content="Tiếng Pháp"/>
                </ComboBox>

                

            </StackPanel>

            <!-- 3. Footer Buttons -->
            <Grid Grid.Row="2" Margin="0,25,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Nút Hủy -->
                <Button Content="Hủy bỏ" Command="{Binding CancelEditTeacherCommand}" 
                        Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Foreground="#374151" 
                        Height="40" Margin="0,0,5,0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Nút Lưu -->
                <Button Content="Lưu thay đổi" Command="{Binding SaveEditTeacherCommand}" Grid.Column="1" 
                        Background="{StaticResource PrimaryBlue}" Foreground="White" BorderThickness="0" 
                        Height="40" Margin="5,0,0,0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>