<UserControl x:Class="GUI.Views.Pages.CourseManagement.CourseManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             
             xmlns:vm="clr-namespace:GUI.ViewModels"
             xmlns:local="clr-namespace:GUI.Views.Pages.CourseManagement"
             
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F3F4F6" FontFamily="Segoe UI">

    <!-- (Resources giữ nguyên) -->
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="OrangeBg" Color="#F97316"/>
        <Style x:Key="CenterDataGridTextColumn" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GridActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <TextBlock Text="{TemplateBinding Content}" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" Foreground="#6B7280"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <UserControl.DataContext>
        <vm:CourseManagementViewModel/>
    </UserControl.DataContext>

    <Grid>
        <!-- LỚP 1: MÀN HÌNH CHÍNH -->
        <Grid Margin="20">
            <!-- (Header, Toolbar giữ nguyên) -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Danh sách khóa học" FontSize="24" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,20"/>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="300" Height="40" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8">
                    <Grid>
                        <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#9CA3AF"/>
                        <TextBox Text="Tìm kiếm khóa học..." VerticalAlignment="Center" Margin="35,0,10,0" BorderThickness="0" Background="Transparent" Foreground="#6B7280"/>
                    </Grid>
                </Border>
                <StackPanel Orientation="Horizontal" Grid.Column="2">
                    <Button Command="{Binding OpenAddDialogCommand}" Content="Thêm khóa học" Background="{StaticResource PrimaryBlue}" Foreground="White" Style="{StaticResource ToolbarButtonStyle}"/>
                </StackPanel>
            </Grid>

            <!-- DataGrid -->
            <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="0" BorderBrush="#E5E7EB" BorderThickness="1" ClipToBounds="True">
                <DataGrid ItemsSource="{Binding Courses}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F3F4F6" BorderThickness="0" Background="White" RowHeight="60">

                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="15,12"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.CellStyle>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="MÃ KH" Binding="{Binding CourseID}" Width="80" Foreground="#6B7280" FontFamily="Consolas" ElementStyle="{StaticResource CenterDataGridTextColumn}"/>
                        <DataGridTemplateColumn Header="TÊN KHÓA HỌC" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding CourseName}" FontWeight="Bold" FontSize="14" Foreground="#111827"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="THỜI LƯỢNG" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="#EFF6FF" BorderBrush="#DBEAFE" BorderThickness="1" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding DurationMonths, StringFormat='{}{0} Tháng'}" FontSize="12" FontWeight="Bold" Foreground="#1D4ED8"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="HỌC PHÍ CƠ BẢN" Binding="{Binding BaseFee, StringFormat='{}{0:N0} VNĐ'}" Width="1.5*" FontWeight="Bold" Foreground="#111827" ElementStyle="{StaticResource CenterDataGridTextColumn}"/>

                        <!-- CỘT HÀNH ĐỘNG -->
                        <DataGridTemplateColumn Header="HÀNH ĐỘNG" Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">

                                        <!-- Nút Sửa: Gắn Command OpenEditDialog -->
                                        <Button Content="&#xE70F;" ToolTip="Chỉnh sửa" Style="{StaticResource GridActionButton}" Foreground="#D97706"
                                                Command="{Binding DataContext.OpenEditDialogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>

                                        <!-- Nút Xóa -->
                                        <Button Content="&#xE74D;" ToolTip="Xóa" Style="{StaticResource GridActionButton}" Foreground="#DC2626"
                                                Command="{Binding DataContext.OpenDeleteDialogCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- LỚP 2: POPUP THÊM (Overlay) -->
        <Grid Visibility="{Binding IsAddPopupOpen, Converter={StaticResource BoolToVis}}" Panel.ZIndex="99">
            <Border Background="Black" Opacity="0.4"/>
            <local:AddCourseView HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <!-- LỚP 3: POPUP XÓA (Overlay) -->
        <Grid Visibility="{Binding IsDeletePopupOpen, Converter={StaticResource BoolToVis}}" Panel.ZIndex="99">
            <Border Background="Black" Opacity="0.4"/>
            <local:DeleteCourseView HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>

        <!-- LỚP 4: POPUP SỬA (Overlay - MỚI THÊM) -->
        <Grid Visibility="{Binding IsEditPopupOpen, Converter={StaticResource BoolToVis}}" Panel.ZIndex="99">
            <Border Background="Black" Opacity="0.4"/>
            <!-- Nhúng form Edit vào đây -->
            <local:EditCourseView HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>