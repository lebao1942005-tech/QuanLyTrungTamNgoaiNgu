<UserControl x:Class="GUI.Views.Pages.CourseManagement.AddCourseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Width="450" Height="Auto"
             Background="Transparent" FontFamily="Segoe UI">

    <UserControl.Resources>
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1D4ED8"/>
        <!-- (Giữ nguyên Style TextBox cũ của bạn ở đây) -->
        <Style TargetType="TextBox">
            <!-- ... Code Style cũ ... -->
            <Setter Property="Padding" Value="10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="12" BorderBrush="#E5E7EB" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2" Color="Gray"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Thêm khóa học mới" FontSize="20" FontWeight="Bold" Foreground="#111827" VerticalAlignment="Center"/>
                <!-- Binding Command Hủy -->
                <Button Command="{Binding CancelAddCommand}" Content="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12" Width="30" Height="30" Background="Transparent" BorderThickness="0" HorizontalAlignment="Right" Cursor="Hand"/>
            </Grid>

            <!-- Form -->
            <StackPanel Grid.Row="1" VerticalAlignment="Top">
                <TextBlock Text="Tên khóa học" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                <!-- Binding NewCourseName -->
                <TextBox Text="{Binding NewCourseName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Thời lượng (Tháng)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <!-- Binding NewDurationMonths -->
                        <TextBox Text="{Binding NewDurationMonths, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Học phí cơ bản (VNĐ)" FontWeight="SemiBold" Margin="0,0,0,5" Foreground="#374151"/>
                        <!-- Binding NewBaseFee -->
                        <TextBox Text="{Binding NewBaseFee, StringFormat=N0, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" Foreground="#F59E0B" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="Học phí này sẽ được áp dụng tự động khi tạo lớp mới." Foreground="#6B7280" FontSize="12" FontStyle="Italic" TextWrapping="Wrap" Width="350"/>
                </StackPanel>
            </StackPanel>

            <!-- Footer -->
            <Grid Grid.Row="2" Margin="0,25,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Nút Hủy -->
                <Button Content="Hủy bỏ" Command="{Binding CancelAddCommand}" Background="White" BorderBrush="#D1D5DB" BorderThickness="1" Foreground="#374151" Height="40" Margin="0,0,5,0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Nút Lưu -->
                <Button Content="Lưu khóa học" Command="{Binding SaveNewCourseCommand}" Grid.Column="1" Background="{StaticResource PrimaryBlue}" Foreground="White" BorderThickness="0" Height="40" Margin="5,0,0,0" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>