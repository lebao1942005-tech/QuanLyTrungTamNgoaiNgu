<UserControl x:Class="GUI.Views.Pages.ClassManagement.ClassStudentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900"
             Background="Transparent" FontFamily="Segoe UI">

    <UserControl.Resources>
        <!-- COLORS -->
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="GreenText" Color="#16A34A"/>
        <SolidColorBrush x:Key="GreenBg" Color="#DCFCE7"/>
        <SolidColorBrush x:Key="GrayText" Color="#6B7280"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E5E7EB"/>
        <SolidColorBrush x:Key="HoverColor" Color="#F9FAFB"/>

        <!-- STYLE: ICON BUTTON (Sửa/Xóa) -->
        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <!-- Container chính: Card bo góc trắng -->
    <Border Background="White" CornerRadius="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Toolbar -->
                <RowDefinition Height="*"/>
                <!-- DataGrid -->
            </Grid.RowDefinitions>

            <!-- 1. TOOLBAR: Tiêu đề & Nút Thêm -->
            <Grid Grid.Row="0" Margin="24,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Tiêu đề danh sách -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Danh sách học viên" FontSize="16" FontWeight="Bold" Foreground="#111827"/>
                    <TextBlock Text="(15/20)" FontSize="16" FontWeight="Medium" Foreground="{StaticResource GrayText}" Margin="8,0,0,0"/>
                </StackPanel>

                <!-- Nút Thêm vào lớp -->
                <Button Grid.Column="1" 
                        Content="+ Thêm vào lớp" 
                        Background="{StaticResource PrimaryBlue}" Foreground="White" 
                        FontSize="13" FontWeight="SemiBold"
                        Padding="16,8" BorderThickness="0" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter Margin="{TemplateBinding Padding}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Opacity" Value="0.9"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- 2. DATAGRID: Danh sách -->
            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding ClassStudents}" 
                      AutoGenerateColumns="False" CanUserAddRows="False" 
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F3F4F6"
                      BorderThickness="0"
                      Background="White"
                      RowHeight="60"
                      Padding="0,0,0,10">

                <!-- Style cho Header (Tiêu đề cột) -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="#9CA3AF"/>
                        <!-- Màu xám nhạt -->
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="24,0,0,10"/>
                        <!-- Padding trái để căn thẳng hàng -->
                        <Setter Property="Height" Value="40"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="VerticalContentAlignment" Value="Bottom"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <!-- Style cho Cell (Ô dữ liệu) -->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="24,0"/>
                        <!-- Căn lề trái cho nội dung -->
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderThickness="0" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>

                <!-- Style cho Row (Hàng) -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource HoverColor}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <!-- Cột 1: MÃ HV -->
                    <DataGridTextColumn Header="MÃ HV" Binding="{Binding StudentId}" Width="140" 
                                        Foreground="{StaticResource GrayText}" 
                                        ElementStyle="{StaticResource {x:Type TextBlock}}"/>

                    <!-- Cột 2: HỌ TÊN (In đậm) -->
                    <DataGridTemplateColumn Header="HỌ TÊN" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="#111827" FontSize="14"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Cột 3: NGÀY SINH -->
                    <DataGridTextColumn Header="NGÀY SINH" Binding="{Binding DateOfBirth, StringFormat='dd/MM/yyyy'}" Width="160" 
                                        Foreground="{StaticResource GrayText}"/>

                    <!-- Cột 4: SĐT -->
                    <DataGridTextColumn Header="SĐT" Binding="{Binding Phone}" Width="160" 
                                        Foreground="{StaticResource GrayText}"/>

                    <!-- Cột 5: TRẠNG THÁI (Badge) -->
                    <DataGridTemplateColumn Header="TRẠNG THÁI" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource GreenBg}" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource GreenText}"/>
                                </Border>
                                <!-- Bạn có thể thêm DataTrigger để đổi màu badge nếu trạng thái khác 'Đang học' -->
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Cột 6: HÀNH ĐỘNG (Sửa/Xóa) -->
                    <DataGridTemplateColumn Header="HÀNH ĐỘNG" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <!-- Nút Sửa -->
                                    <Button Style="{StaticResource IconButtonStyle}" ToolTip="Chỉnh sửa" Margin="0,0,4,0">
                                        <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#D97706"/>
                                    </Button>

                                    <!-- Nút Xóa -->
                                    <Button Style="{StaticResource IconButtonStyle}" ToolTip="Xóa khỏi lớp">
                                        <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#EF4444"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Border>
</UserControl>