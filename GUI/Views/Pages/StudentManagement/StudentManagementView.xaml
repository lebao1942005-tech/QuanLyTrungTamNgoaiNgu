<UserControl x:Class="GUI.Views.Pages.StudentManagement.StudentManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views.Pages.StudentManagement"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="#F3F4F6" FontFamily="Segoe UI">
    <UserControl.Resources>
        <!-- ================= COLORS ================= -->
        <SolidColorBrush x:Key="PrimaryBlue" Color="#1D4ED8"/>
        <SolidColorBrush x:Key="LightBlueBg" Color="#DBEAFE"/>
        <SolidColorBrush x:Key="GreenText" Color="#059669"/>
        <SolidColorBrush x:Key="GreenBg" Color="#D1FAE5"/>
        <SolidColorBrush x:Key="PinkText" Color="#DB2777"/>
        <SolidColorBrush x:Key="PinkBg" Color="#FCE7F6"/>
        <SolidColorBrush x:Key="YellowText" Color="#D97706"/>
        <SolidColorBrush x:Key="YellowBg" Color="#FEF3C7"/>
        <SolidColorBrush x:Key="RedText" Color="#DC2626"/>
        <SolidColorBrush x:Key="OrangeBg" Color="#F97316"/>

        <!-- ================= STYLES ================= -->

        <!-- Style cho nút Toolbar -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="{TemplateBinding Tag}" FontFamily="Segoe MDL2 Assets" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.9"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style cho Icon Button trong bảng (Xem, Sửa, Xóa) -->
        <Style x:Key="GridActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15">
                            <TextBlock Text="{TemplateBinding Content}" FontFamily="Segoe MDL2 Assets" 
                                       VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16" Foreground="#6B7280"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="Auto"/>
            <!-- Toolbar -->
            <RowDefinition Height="*"/>
            <!-- Table -->
        </Grid.RowDefinitions>

        <!-- 1. Header -->
        <TextBlock Text="Danh sách học viên" FontSize="24" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,20"/>

        <!-- 2. Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- Filter & Search -->
                <ColumnDefinition Width="*"/>
                <!-- Spacer -->
                <ColumnDefinition Width="Auto"/>
                <!-- Action Buttons -->
            </Grid.ColumnDefinitions>

            <!-- Filter & Search -->
            <StackPanel Orientation="Horizontal" Grid.Column="0">
                <!-- Filter Button -->
                <Button Width="40" Height="40" Background="White" BorderBrush="#E5E7EB" BorderThickness="1">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="8">
                                <TextBlock Text="&#xE71C;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#6B7280"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Search Box -->
                <Border Margin="10,0,0,0" Width="300" Height="40" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="8">
                    <Grid>
                        <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="#9CA3AF"/>
                        <TextBox Text="Tìm kiếm" VerticalAlignment="Center" Margin="35,0,10,0" BorderThickness="0" Background="Transparent" Foreground="#6B7280"/>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- Action Buttons -->
            <StackPanel Orientation="Horizontal" Grid.Column="2">
                <!-- Nút Thêm -->
                <Button Content="Thêm" Tag="&#xE710;" Background="{StaticResource PrimaryBlue}" Foreground="White" Style="{StaticResource ToolbarButtonStyle}"/>

                <!-- Nút Xuất file -->
                <Button Content="Xuất file" Tag="&#xE78C;" Background="{StaticResource OrangeBg}" Foreground="White" Style="{StaticResource ToolbarButtonStyle}"/>
            </StackPanel>
        </Grid>

        <!-- 3. Data Grid -->
        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="0" BorderBrush="#E5E7EB" BorderThickness="1" ClipToBounds="True">
            <DataGrid ItemsSource="{Binding Students}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F3F4F6"
                      BorderThickness="0"
                      Background="White"
                      RowHeight="70">

                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="15,12"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <!-- Cột 2: Thông tin (Avatar + Tên + ID) -->
                    <DataGridTemplateColumn Header="THÔNG TIN" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <!-- Giả lập Avatar -->
                                    <Border Width="40" Height="40" CornerRadius="20" Background="#E5E7EB" Margin="0,0,12,0">
                                        <TextBlock Text="{Binding NameInitial}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="#6B7280"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="#111827"/>
                                        <TextBlock Text="{Binding StudentId}" FontSize="12" Foreground="#6B7280" Margin="0,2,0,0"/>
                                        <TextBlock Text="{Binding StudentBirthDay}" FontSize="12" Foreground="#6B7280" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Cột 3: Liên hệ -->
                    <DataGridTemplateColumn Header="LIÊN HỆ" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Phone, StringFormat='Điện thoại: {0}'}" FontSize="13" Foreground="#374151"/>
                                    <TextBlock Text="{Binding Email, StringFormat='Email: {0}'}" FontSize="13" Foreground="#6B7280" Margin="0,2,0,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Cột 5: Trạng thái -->
                    <DataGridTemplateColumn Header="TRẠNG THÁI" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource LightBlueBg}" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Status}" Foreground="{StaticResource PrimaryBlue}" FontSize="12" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Cột 6: Trạng thái học -->
                    <DataGridTemplateColumn Header="TRẠNG THÁI HỌC" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource YellowBg}" CornerRadius="12" Padding="10,4" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding LearningStatus}" Foreground="{StaticResource YellowText}" FontSize="12" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Cột 7: Chức năng -->
                    <DataGridTemplateColumn Header="CHỨC NĂNG" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="&#xE7B3;" ToolTip="Xem chi tiết" Style="{StaticResource GridActionButton}"/>
                                    <Button Content="&#xE70F;" ToolTip="Chỉnh sửa" Style="{StaticResource GridActionButton}" Foreground="#D97706"/>
                                    <Button Content="&#xE74D;" ToolTip="Xóa" Style="{StaticResource GridActionButton}" Foreground="#DC2626"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
